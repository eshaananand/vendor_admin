import 'package:flutter/material.dart';

class GlobalIncome extends StatelessWidget {
  GlobalIncome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("GlobalIncome"),
      ),
    );
  }
}
